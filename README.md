The python program is a simple script that will force download and launch discord across it's different versions (ptb, canary, stable)
Made as I found it annoying having to go to the page every time and didn't want to use a flatpak or anything else.  

To run, simply install the packages and run.  By default, it will download canary into the current directory you run the python program.  
